import { RouterConfig } from '@angular/router';
import { SkillsComponent } from './skills.component';

export const SkillsRoutes : RouterConfig = [
  { path: 'skills',  component: SkillsComponent }
];
